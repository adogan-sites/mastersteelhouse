/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // basePath: "/mastersteelhouse",
    images: {
        unoptimized: true,
    }
};

export default nextConfig;
